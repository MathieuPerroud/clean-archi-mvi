<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/size_small_dim"
    android:paddingHorizontal="@dimen/size_small_dim"
    >
    <TextView
        android:id="@+id/name"
        android:layout_width="match_parent"
        android:layout_height="48dp"

        android:background="@drawable/ripple_white_bg"
        android:backgroundTint="#77000000"
        android:clickable="true"
        android:drawableStart="@drawable/ic_house"
        android:drawablePadding="32dp"
        android:focusable="true"
        android:gravity="start|center_vertical"
        android:paddingStart="@dimen/size_medium_dim"
        android:paddingEnd="@dimen/size_medium_dim"
        tools:text="Salon Napoléon"
        android:textColor="@color/color_on_primary"
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/availability"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/size_medium_dim"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:maxLines="2"
        android:textColor="@color/color_on_primary"

        tools:text="Disponible"
        />
</androidx.constraintlayout.widget.ConstraintLayout>